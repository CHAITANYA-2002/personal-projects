# Runtime dependencies for the Streamlit dashboard (imarticus projects/sql/app.py).
#
# This file lives at the repository root because Streamlit Community Cloud does not
# quote the requirements path, so the space in the "imarticus projects" directory
# name breaks the install when the file sits next to the app.
#
# Notebook-only dependencies are in requirements-notebooks.txt.
streamlit>=1.40
pandas>=2.0
numpy>=1.24
plotly>=5.20
SQLAlchemy>=2.0
PyMySQL>=1.1
